#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"

#include "ml/core.hpp"

// Placeholder test so the empty scaffold has something to build and run.
// Delete once real ml-core tests exist.
TEST_CASE("scaffold sanity check") {
    CHECK(1 + 1 == 2);
}
