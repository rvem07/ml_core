# Benchmarks placeholder. No benchmark targets or framework are wired up
# yet — this directory exists to reserve the layout. Enable with
# -DML_CORE_BUILD_BENCHMARKS=ON once real benchmarks are added here.
